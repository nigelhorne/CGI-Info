---
module: CGI::Info
function: host_name

new:

# host_name takes no arguments
input: undef

# Returns an absolute path
output:
  type: string
  min: 1

config:
  test_undef: yes
  test_empty: no
  test_nuls: no
  dedup: yes

seed: 44
iterations: 50
